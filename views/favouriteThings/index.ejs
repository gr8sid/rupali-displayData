<% include ../partials/header.ejs %>
 
<!-- embed my favourite thhings content here -->
    <div class="container">
            <h1> Sidharth's <%= title %></h1>
        <div class="row">
            <div class="col-md-offset-3 col-md-6"></div>
        
        <br>
        <table class="table table-bordered table-striped table-hover">
           
           <!-- Header Row --> 
           <tr>
                <th class="text-centre"> Name </th>
                <th class="text-centre"> Description </th>
            </tr>
            <% for(let count=0; count < favouriteThingsList.length; count++) { %>
            <tr>
                <th class="text-centre"> <%= favouriteThingsList[count].name %></th>
                <th class="text-centre">  <%= favouriteThingsList[count].description  %></th>
            </tr>
            <% } %>
            </table>
        </div>
    </div>
<% include ../partials/footer.ejs %>